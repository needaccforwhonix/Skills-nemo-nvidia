# Pipeline/orchestration dependencies (CLI, cluster management, experiment tracking).
# These are additional to core/requirements.txt.

nemo-evaluator-launcher<0.1.47
nemo_run @ git+https://github.com/NVIDIA-NeMo/Run
# >=0.16 is click 8.2 compatible (fixes the make_metavar break behind
# https://github.com/ai-dynamo/dynamo/issues/1039).
# <0.27 because typer 0.27.0 generates dash-separated option names (--output-dir)
# instead of the underscore names (--output_dir) that every `ns` command, test and
# doc example uses. Lift the cap only together with a repo-wide rename.
typer >= 0.16, < 0.27
